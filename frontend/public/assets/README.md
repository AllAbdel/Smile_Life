# Assets Audio

Place le fichier **alice.mp3** ou **alice.wav** ici.

Le fichier sera accessible via `/assets/alice.mp3` dans l'application.

## Instructions

1. Copie ton fichier audio (alice.mp3 ou alice.wav)
2. Place-le dans ce dossier
3. Renomme-le en `alice.mp3` si ce n'est pas déjà le cas
4. Le bouton "Alice 💕" dans la soundboard jouera ce fichier

**Note** : Le fichier n'est pas inclus dans Git pour des raisons de taille.
